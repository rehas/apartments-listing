export const baseUrl =  'http://localhost:4000'

export const localStorageJwtKey = 'currentUserJwt'

// Use if deployed

/* process.env.REACT_APP_ENV === 'production' ? 'https://apartments-app-api.herokuapp.com' : */
